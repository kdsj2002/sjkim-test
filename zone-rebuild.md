# zone-rebuild.sh

APEX 도메인 하나를 넣으면 공개 DNS 응답만 긁어서 zone 파일 형태로 뱉는 스크립트다.
운영 중인 도메인의 존 설정을 넘겨받지 못했을 때, 대략 어떤 레코드가 살아 있는지
파악하는 용도로 쓴다.

```
./zone-rebuild.sh example.com          # example.com.zone 생성
./zone-rebuild.sh -o - example.com     # 표준출력
```

## 동작 순서

1. apex 레코드(SOA, NS, A, AAAA, MX, TXT, CAA, DNSKEY)를 조회한다.
2. NS 목록으로 AXFR을 한 번씩 찔러본다. 열려 있으면 받아온 존을 그대로 쓰고 끝낸다.
   (`dig`이 있을 때만. DoH로는 존 전송이 안 된다.)
3. 막혀 있으면 랜덤 라벨을 몇 번 물어서 와일드카드가 걸려 있는지 확인한다.
4. 후보 라벨 목록으로 2차 도메인을 추측 조회한다.
5. 얻은 응답을 고유 레코드와 중복으로 갈라서 zone 파일을 만든다.

## 와일드카드 중복 처리

`*.example.com`이 걸려 있으면 등록되지도 않은 이름이 전부 같은 값으로 응답한다.
이걸 그대로 받아 적으면 존재하지 않는 레코드 수백 개짜리 zone 파일이 나온다.
그래서 다음 순서로 접는다.

**1) 와일드카드 값 수집.** 존재할 리 없는 랜덤 라벨을 타입마다 4번(`--wildcard-probes`)
물어본다. 매번 답이 오면 와일드카드가 있는 것으로 보고, 돌아온 값을 전부 모아
합집합으로 둔다. 로테이션으로 값이 바뀌어도 합집합에 들어간다.

**2) 값 비교는 타입 단위로.** 어떤 이름의 특정 타입 응답값이 전부 그 합집합 안에
들어 있으면 와일드카드가 만들어낸 응답이므로 버린다. 값이 하나라도 밖에 있으면
실제로 등록된 레코드로 본다. 타입 단위라서 `blog`가 A는 와일드카드를 타고 TXT만
따로 갖고 있으면 TXT는 살아남는다.

**3) 비교 전 값 정규화.**
- TTL은 뺀다. 캐시 상태에 따라 흔들리기 때문에 값 비교에 쓸 수 없다.
- `*.d.com CNAME <라벨>.cdn.net` 처럼 질의한 라벨을 되돌려주는 구성이 있어서,
  값의 맨 앞 라벨 자리가 질의한 이름과 같으면 `{LABEL}`로 바꿔 놓고 비교한다.
  이름 전체가 아니라 라벨 경계만 본다. 부분 문자열까지 바꾸면 `mail`이
  `include:mail.zendesk.com`을 건드려서 멀쩡한 값이 서로 달라 보인다.

**4) 랜덤 라벨에 안 걸린 경우.** 와일드카드가 리졸버 캐시 사정으로 한쪽 값만
돌려줘서 3번까지 못 걸러내는 경우가 있다. 그래서 마지막에 서로 다른 이름 8개
이상(`--dup-threshold`)이 완전히 같은 값을 준 그룹을 찾는다. 같은 타입에 이미
와일드카드가 잡혀 있었다면 그 와일드카드가 값을 돌려 쓰는 것으로 보고 `*` 쪽에
합치고, 아니면 catch-all이나 파킹으로 보고 따로 표시한다. `--dup-threshold 0`으로
끌 수 있다.

접힌 이름은 버리지 않고 zone 파일 끝에 주석으로 전부 남긴다. `--no-wildcard`를
주면 이 과정을 통째로 건너뛰고 받은 응답을 그대로 출력한다.

surge.sh 실행 예 - 후보 209개가 전부 와일드카드 응답이라 개별 레코드는 하나도
남지 않았다.

```
; ---- wildcard ----
*                          3360    IN A       159.203.50.177
*                          3600    IN A       159.203.159.100

; ---- subdomains ----
; 고유한 응답을 준 2차 도메인이 없다

; ---- 중복 처리 ----
; 응답은 왔지만 값이 와일드카드/공통 응답과 같아 개별 레코드로 쓰지 않은 이름 (209개):
;   _dmarc _domainkey account accounts ad admin administrator alpha api api2 app apps ...
```

## 리졸버

`dig`이 있으면 dig을 쓰고, 없으면 DoH(HTTPS)로 붙는다. `-r`로 강제할 수 있다.
DoH는 dns.google을 먼저 쓰고 실패하면 cloudflare-dns.com으로 넘어간다.
jq가 있으면 jq로, 없으면 python3로 응답을 판다.

제공자마다 TXT 인용부호를 붙이기도 하고 안 붙이기도 해서, 안 붙어 있으면
이스케이프 후 255바이트 단위로 잘라 인용한다. 같은 CNAME이 타입별 질의마다
TTL만 다르게 잡히는 것도 하나로 합친다.

## 옵션

| 옵션 | 설명 |
|---|---|
| `-o, --output FILE` | 출력 파일. 기본 `<domain>.zone`, `-`면 표준출력 |
| `-w, --wordlist FILE` | 후보 라벨 목록. 없으면 내장 목록 209개 |
| `-t, --types LIST` | 서브도메인 조회 타입. 기본 `A,AAAA,TXT,MX` (CNAME은 자동) |
| `--apex-types LIST` | apex 조회 타입 |
| `-r, --resolver MODE` | `auto` / `dig` / `doh` |
| `-s, --server ADDR` | dig 모드에서 쓸 네임서버 |
| `-j, --jobs N` | 동시 질의 수. 기본 8 |
| `--timeout SEC` | 질의 타임아웃. 기본 5 |
| `--dup-threshold N` | 동일 응답 그룹 판정 기준. 기본 8, 0이면 끔 |
| `--wildcard-probes N` | 와일드카드 확인용 랜덤 질의 횟수. 기본 4 |
| `--no-wildcard` | 중복 제거 없이 전부 개별 출력 |
| `--no-axfr` | AXFR 시도 생략 |
| `-q` / `-v` | 로그 끄기 / 질의 단위 로그 |

## 알아둘 것

- 공개 응답만 보는 것이라 실제 존과 같지 않다. 워드리스트에 없는 이름은 안 나오고,
  내부 전용 레코드도 안 나온다. 결과 파일 헤더에도 같은 내용을 적어 둔다.
- SOA 응답이 없으면 자리표시자 SOA를 넣고 주석으로 표시한다. 그대로 로드하지 말 것.
- 와일드카드가 질의 라벨을 되돌려주는 구성이면 `*` 레코드 값에 라벨 자리가 남는다.
  zone 파일 문법으로는 쓸 수 없는 값이라 그 줄만 주석으로 내려 둔다.
- 본인이 관리하거나 조회 허가를 받은 도메인에만 쓸 것. 후보 목록이 커지면
  질의량이 그만큼 늘어난다.

## 요구사항

bash 4 이상, awk, sed, coreutils. dig 모드면 `dig`, DoH 모드면 `curl`과
jq 또는 python3.
